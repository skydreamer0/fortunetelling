{
  "name": "fortunetelling",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "bun run --filter @fortune/web dev",
    "build": "bun run --filter @fortune/web build",
    "preview": "bun run --filter @fortune/web preview",
    "test": "bun test"
  }
}
